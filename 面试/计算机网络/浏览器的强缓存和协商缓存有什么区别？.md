
浏览器缓存主要分为两类：
1. **强缓存（Strong Cache）**：浏览器直接使用本地缓存，不请求服务器
2. **协商缓存（Negotiation Cache）**：浏览器询问服务器资源是否变化，如果没变化，继续使用本地缓存。

强缓存是浏览器直接判断资源是否过期，如果缓存有效，则直接使用本地缓存，不会向服务器发送请求，主要通过 Cache-Control 和 Expires 控制。

协商缓存是在强缓存失效后，浏览器携带缓存标识向服务器询问资源是否发生变化，服务器根据 ETag 或 Last-Modified 判断，如果没有变化返回 304，浏览器继续使用缓存；如果变化则返回新的 200 资源。

实际项目中通常对带 hash 的静态资源设置长期强缓存，而 HTML 使用协商缓存或 no-cache 保证及时更新。
